<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.test.mybatis.MyMapper">

    <select id="selectData"  parameterType="hashMap" resultType="hashMap">
    <!-- 쿼리문 -->
    select * from data;
    </select>
   
    <insert id="insertData" parameterType="String">
    <!-- 쿼리문 -->    
        insert data(data0, data1, data2) values (#{data0}, #{data1}, #{data2});
    </insert>   
    
    <insert id="updateData" parameterType="hashMap">
    <!-- 쿼리문 -->    
    </insert>   

    <insert id="deleteData" parameterType="hashMap">
    <!-- 쿼리문 -->    
    </insert>   
</mapper>  